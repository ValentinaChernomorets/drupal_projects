/**
 * @file
 * transformyou behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.transformyou = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
