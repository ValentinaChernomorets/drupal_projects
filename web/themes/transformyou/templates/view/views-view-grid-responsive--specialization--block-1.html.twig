{# HTML for cpecialization grid #}
<div class="specialization-grid">
  {% for row in items %}
    <div class="specialization-item">
      {# row.content  insist all HTML one item-а #}
      {{ row.content }}
    </div>
  {% endfor %}
</div>